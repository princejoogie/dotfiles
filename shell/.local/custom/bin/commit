#!/bin/sh

opencode2 run --model "xai/grok-4.6#low" "Stage all unstaged changes in the current git repository, then analyze all changes to generate a commit message following the Conventional Commits specification. Finally, execute the git commit. Do not push the changes."
